# /watch 配置文件示例
#
# 复制本文件为 .env 并根据实际情况修改：
#   Windows: 复制到 C:\Users\<你的用户名>\.config\watch\.env
#   macOS/Linux: 复制到 ~/.config/watch/.env
#
# 也可以直接运行 `py ./scripts/setup.py` 自动生成配置文件。
#
# 本地转写使用 FunASR + SenseVoiceSmall（阿里达摩院开源模型）
# 无需 API key，完全离线运行（首次运行需下载模型约 234MB）
#
# 安装依赖：
#   pip install funasr torch
# GPU 加速（可选，需 NVIDIA 显卡）：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# FunASR 文档：https://github.com/modelscope/FunASR

# ========== 转写设备配置 ==========
# auto  - 自动检测（有 GPU 用 GPU，否则用 CPU）【推荐】
# cuda  - 强制用 GPU
# cpu   - 强制用 CPU
WATCH_TRANSCRIBE_DEVICE=auto

# ========== 默认观看模式 ==========
# 首次运行向导会询问并自动设置此项
# Allowed values: transcript | efficient | balanced | token-burner
# transcript      - 仅获取字幕，不抽帧（省 Token）
# efficient       - 快速关键帧抽取（上限 50 帧）
# balanced        - 场景感知抽帧（上限 100 帧）【推荐】
# token-burner    - 场景感知抽帧，不设上限（最高保真，高 Token 消耗）
WATCH_DETAIL=balanced

# ========== yt-dlp Cookie 配置 ==========
# 用于下载需要登录的视频（如 B站会员视频、抖音会员视频等）
#
# 方式一：指定 Cookie 文件路径（Netscape 格式）
#   推荐使用浏览器扩展 J2Team Cookies 导出 cookies.txt
#   Chrome: https://chrome.google.com/webstore/detail/j2team-cookies
WATCH_COOKIE_FILE=

# 方式二：使用浏览器自动读取 Cookie（不推荐，可能不稳定）
# 指定浏览器名称：chrome | edge | firefox | safari | brave
# WATCH_BROWSER=edge

# ========== Python 解释器路径（仅 Windows + Trae 环境需要） ==========
# Trae 自带的 Python 3.10 缺少 FunASR
# setup.py 会自动检测含 FunASR 的系统 Python 路径并写入此处
# 如果留空，则使用默认的 py 启动器
WATCH_PYTHON=

# ========== 安装完成标记 ==========
# setup.py 检测到所有依赖就绪后会自动设为 true
# 避免每次运行都触发首次运行向导
SETUP_COMPLETE=false
